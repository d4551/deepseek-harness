/**
 * Hosted network-drive profile layer. The bundle carries only
 * `cordis.patch.yml`: it inserts the WebDAV drive and the drive-backed
 * filesystem provider, disables the host-local filesystem row, and repoints the
 * sandbox workspace root at the materialization directory.
 *
 * @module @deepseek-ai/dsh-hosted-drive
 */

export {}
